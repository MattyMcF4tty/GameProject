#ifndef _DISPLAY_LCD_H_
#define _DISPLAY_LCD_H_

	/* ----- Dependencies ----- */
	#include <stdint.h>
	#include "charset.h"

	/* ----- Functions ----- */
	const uint8_t *charToBuffer(char c);

#endif

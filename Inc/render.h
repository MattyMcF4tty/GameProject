#ifndef _RENDER_H_
#define _RENDER_H_


	/* ----- Dependencies ----- */
	#include <stdint.h>
	#include <stdio.h>
	#include "ansi.h"
	#include "sprites.h"
	#include "game_master.h"

 	/* ----- Type definitions ----- */

	/* ----- Functions ----- */
	void drawSpaceship(const spaceship_t *ship);

#endif
